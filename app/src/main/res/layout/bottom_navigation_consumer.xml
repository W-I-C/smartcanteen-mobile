<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation_consumer"
        android:layout_width="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_height="60dp"
        android:elevation="4dp"
        android:background="@color/white"
        app:menu="@menu/consumer_bottom_navigation_menu"
        app:labelVisibilityMode="unlabeled"
        app:itemIconSize="30dp"
        app:itemIconTint="@color/background_color" />

    <FrameLayout
        android:id="@+id/consumer_fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="60dp"
        android:background="@color/white"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation_consumer"></FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>