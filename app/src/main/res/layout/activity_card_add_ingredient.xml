<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="130dp"
    android:layout_marginTop="20dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:elevation="5dp"
    android:background="@drawable/my_cart_order"
    >



    <ImageView
        android:id='@+id/delete'
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/delete_button"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />


    <TextView
        android:paddingTop="20dp"
        android:paddingLeft="20dp"
        android:id="@+id/name_ingrediente"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="Nome ingrediente"
        android:textColor="@color/black"
        android:layout_marginTop="10dp"
        android:layout_marginLeft="10dp"

        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:paddingLeft="20dp"
        android:id="@+id/ingredient_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:text="Tipo:"
        android:textColor="#808080"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@id/name_ingrediente"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Apenas decrementar"
        android:layout_marginTop="10dp"
        android:paddingLeft="10dp"
        android:textSize="14sp"
        app:layout_constraintLeft_toRightOf="@+id/ingredient_name"
        app:layout_constraintTop_toBottomOf="@+id/name_ingrediente"
        android:textColor="@color/background_color"/>


</androidx.constraintlayout.widget.ConstraintLayout>